@model IEnumerable<WebHospital.Models.Estadopaciente>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.estadodelpaciente)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fecha_estadop)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.estadodelpaciente)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fecha_estadop)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idEstadopaciente }) |
            @Html.ActionLink("Details", "Details", new { id=item.idEstadopaciente }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idEstadopaciente })
        </td>
    </tr>
}

</table>
